<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @post, local: true, url: posts_path) do |f| %>
  <%= f.hidden_field :title %> <!--入力フォームを生成せず、new Viewからきた値をそのままconfirmアクションに渡すようにしている-->
  <%= f.hidden_field :content %>
  <%= f.hidden_field :image_path_cache %>

  <p>タイトル:<%= @post.title %></p>
  <p>本文:<%= @post.content %></p>
  <p>画像:<%= image_tag @post.image_path.url %></p>
  <%= f.submit  "投稿する" %>
<% end %>
