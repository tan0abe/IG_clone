<%= form_with(model: @post, local: true) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.email_field :content %>

  <%= f.label :image_path %>
  <%= f.file_field :image_path %>

  <%= f.submit "投稿する" %>
<% end %>